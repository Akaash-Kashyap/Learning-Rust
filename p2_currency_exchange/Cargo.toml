[package]
name = "p2_currency_exchange"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"]}
dialoguer = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
